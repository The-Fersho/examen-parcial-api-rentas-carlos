const config = {
    db: {
      /* don't expose password or any sensitive info, done only for demo */
      host: "localhost",
      user: "root",
      password: "rubiofer50",
      database: "eventos",
    },
    listPerPage: 10,
  };
  module.exports = config;